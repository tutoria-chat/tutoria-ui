/**
 * Course Type Utilities
 *
 * Defines the course type enum used across the application.
 * AI model selection logic is handled entirely by the backend based on
 * the course type and university subscription tier.
 */

export type CourseType = 'math-logic' | 'programming' | 'theory-text';
